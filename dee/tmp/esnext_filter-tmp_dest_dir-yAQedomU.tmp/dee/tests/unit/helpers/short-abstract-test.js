import {
  shortAbstract
} from 'dee/helpers/short-abstract';

module('ShortAbstractHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = shortAbstract(42);
  ok(result);
});
