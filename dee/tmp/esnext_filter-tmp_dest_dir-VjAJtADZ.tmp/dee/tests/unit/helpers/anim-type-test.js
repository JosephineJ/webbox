import {
  animType
} from 'dee/helpers/anim-type';

module('AnimTypeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = animType("something");
  ok(result);
});
